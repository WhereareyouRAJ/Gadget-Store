name: 🚀 Orders CI

on:
  push:
    paths:
      - 'src/orders/**'
    branches:
      - main

jobs:
  call-security-pipeline:
    uses: ./.github/workflows/security-pipeline.yml
    with:
      image_name: "retail-orders"
      context: "./src/orders/"
    secrets: inherit