name: 🚀 Carts CI

on:
  push:
    paths:
      - 'src/carts/**'
    branches:
      - main

jobs:
  call-security-pipeline:
    uses: ./.github/workflows/security-pipeline.yml
    with:
      image_name: "retail-carts"
      context: "./src/carts/"
    secrets: inherit